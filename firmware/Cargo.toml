[package]
name = "firmware"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "firmware"
path = "src/main.rs"

[dependencies]
map-runtime = { path = "../map-runtime" }
render-core = { path = "../render-core" }
runtime-core = { path = "../runtime-core" }
