{
  "id": "agent-persistence",
  "version": "1.0.0",
  "name": "Agent Auth Persistence",
  "description": "Persist Claude, Codex, and Gemini auth directories by mounting host directories and linking them into the remote user home.",
  "mounts": [
    "source=${localEnv:HOME}/.codex,target=/mnt/.codex,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.claude,target=/mnt/.claude,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.gemini,target=/mnt/.gemini,type=bind,consistency=cached"
  ]
}
