{
  "name": "ESP32 Rust Dev",
  "build": {
    "context": "..",
    "dockerfile": "../infra/devcontainer/Containerfile"
  },
  "initializeCommand": "bash .devcontainer/scripts/init-host-home.sh",
  "workspaceFolder": "/work",
  "workspaceMount": "source=${localWorkspaceFolder},target=/work,type=bind",
  "runArgs": [
    "--dns=1.1.1.1",
    "--dns=8.8.8.8"
  ],
  "mounts": [
    "source=${localEnv:HOME}/.devcontainer-homes/esp32-map,target=/home/vscode,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.ssh,target=/home/vscode/.ssh,type=bind,consistency=cached",
    "source=${localEnv:LEX_PATH:-/opt/lex-disabled},target=/opt/lex,type=bind,consistency=cached"
  ],
  "remoteUser": "vscode",
  "containerEnv": {
    "CARGO_TARGET_DIR": "/work/target/devcontainer",
    "GIT_CONFIG_GLOBAL": "/home/vscode/.config/git/config",
    "TZ": "Europe/Helsinki"
  },
  "forwardPorts": [
    5173,
    3024
  ],
  "portsAttributes": {
    "5173": {
      "label": "Emulator (Vite)",
      "onAutoForward": "notify"
    },
    "3024": {
      "label": "Lex Code Viewer",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "bash infra/devcontainer/scripts/post-create.sh",
  "customizations": {
    "vscode": {
      "settings": {
        "rust-analyzer.check.command": "clippy",
        "rust-analyzer.linkedProjects": [
          "/work/firmware/Cargo.toml"
        ],
        "editor.formatOnSave": true,
        "files.trimTrailingWhitespace": true,
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "rust-lang.rust-analyzer",
        "vadimcn.vscode-lldb",
        "tamasfe.even-better-toml",
        "ms-vscode.makefile-tools",
        "probe-rs.probe-rs-debugger",
        "wokwi.wokwi-vscode",
        "fill-labs.dependi",
        "openai.chatgpt"
      ]
    }
  }
}
