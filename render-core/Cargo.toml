[package]
name = "render-core"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
runtime-core = { path = "../runtime-core" }

[build-dependencies]
resvg = { version = "0.47.0", default-features = false }
