# ESP32-P4 partition table for the bike-minimap firmware.
#
# Factory app partition is 4 MB — firmware is ~1 MB so this gives 4x
# headroom. The map_data partition immediately follows, ending exactly at
# the 16 MB boundary (0x1000000). The ESP-IDF generic flash driver caps
# MMU-mmap access at 16 MB, so keeping both partitions below that line
# avoids the "access beyond 16 MB not supported" runtime warning.
#
# Name,    Type, SubType,  Offset,   Size,      Flags
nvs,       data, nvs,      0x9000,   0x6000
phy_init,  data, phy,      0xf000,   0x1000
factory,   app,  factory,  0x10000,  0x3F0000
map_data,  data, 0x80,     0x400000, 0xC00000
